language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-pyapp' ]; then cd .. && eval "mv $currentfolder generator-pyapp" && cd generator-pyapp; fi

